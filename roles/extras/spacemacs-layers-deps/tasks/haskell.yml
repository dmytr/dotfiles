---

- name: Install dependencies via Stack
  shell: "stack install {{ item }}"
  with_items:
    - apply-refact
    - hlint
    - stylish-haskell
    - hasktags
    - hoogle
    - hindent
    - ghc-mod
    - intero
    - hspec-discover
  become: yes
  become_user: "{{ user_name }}"
